# Have to give this a short name to work around micromamba bug
context:
  name: skep
  version: 0.0.1
  github: https://github.com/eyepatchParrot/conda_stitch

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

build:
  number: 0
  noarch: generic
  script:
    - DEST=$PREFIX/share/kak/rc
    - mkdir -p $DEST
    - cp $RECIPE_DIR/evaluate-paragraph.kak $DEST/evaluate-paragraph.kak
    - test -f $DEST/evaluate-paragraph.kak

test:
  exists:
    files:
      - share/kak/rc/evaluate-paragraph.kak

requirements:
  run:
    - stitch-kakoune

about:
  home: '{{ github }}'
  summary: 'Evaluate paragraph'
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE

extra:
  recipe-maintainers:
    - eyepatchParrot
